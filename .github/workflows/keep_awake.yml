name: Keep Bot Awake

on:
  schedule:
    # Runs every 10 minutes, 24/7
    - cron: '*/10 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Ping Signal
        run: curl -I https://your-bot-app.onrender.com/

